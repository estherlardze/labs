import { ReactNode } from 'react'

interface HeaderProps {
    children: ReactNode,
    variant: "h1" | "h2" | "h3" | "h4" | "h5" | "h6";
}
export const Header = ({children, variant, ...props}: HeaderProps) => {
    return <></>
}
